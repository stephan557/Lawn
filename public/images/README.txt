Save the two attachments into this folder with these exact filenames:

  1. logo.png   -> the round "GB Lawnmowing" logo (dark green with the green GB and the lawnmower man)
  2. flyer.jpg  -> the promotional flyer image (Relax! Let the experts handle... / Serving Auckland)

After saving, restart the server (or rebuild the exe with `npm run build`) and refresh the home page.
